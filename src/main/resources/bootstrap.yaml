jsk:
  config:
    client:
      pollingEnabled: true
  spring:
    config:
      idps:
        connection:
          api_key_id: v2-28890d74d4bfa
          api_secret_key_content:
          api_secret_key: /Users/cbhatt1/ius-ppd.pem
          temporary_credentials_renewal_mode: lazy
          key_cache_ttl_seconds: 604800
          audit_crypto: false
          connect_timeout_seconds: 10
          heartbeat_interval_seconds: 20
          stats_report_interval_secs: 300
          endpoint: OII-PRE-PRODUCTION-30BB03.pd.idps.a.intuit.com
  servlet:
    lastmile:
      enabled: false
      truststore-password: '{secret}idps:/ius/secrets/lastmile-truststore-password-ppd'